description: empty
contact: Contact "@matthew.nitschke" via slack
image: drydock.workiva.net/workiva/skynet-images:bash_curl_alpinelatest-latest

size: small
timeout: short

run:
  on-tag: true
  on-pull-request: true

scripts:
  - echo "CI in .github/workflows"
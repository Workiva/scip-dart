name: Scip

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read
  id-token: write

jobs:
  index:
    uses: Workiva/gha-dart/.github/workflows/scip-dart.yaml@master
